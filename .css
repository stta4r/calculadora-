body {
    font-family: Arial, sans-serif;
    background-color: #ffe6f0; /* Rosa claro */
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.calculator {
    background-color: #fff; /* Branco */
    border-radius: 5px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    padding: 20px;
    width: 300px;
}

#display {
    width: calc(100% - 10px);
    margin-bottom: 10px;
    padding: 5px;
    font-size: 24px;
}

.keys {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-gap: 10px;
}

button {
    padding: 10px;
    font-size: 18px;
    border: none;
    cursor: pointer;
    background-color: #ffe6f0; /* Rosa claro */
    outline: none; /* Remove a borda padrão do foco */
}

button:focus {
    border: 2px solid #ff66a3; /* Cor da borda quando em foco */
}

.operator {
    color: #333;
}

